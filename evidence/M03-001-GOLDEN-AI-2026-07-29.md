# M03-001 Deterministic AI Golden Evidence

## Scope

- Module: M03-001
- Date: 2026-07-29
- Writable boundary: `C:\SOL\BattleStarSol-prealpha-02`
- Production AI changes: none

## Golden scenario

Seed `1167583760` constructs an armed pistol Agent at a legal ranged standoff
with reachable protective cover.

The tests prove:

1. `seek_cover` outranks both legal fire and the generic approach fallback.
2. The exact protective route, score 79, and retained attack AP are stable.
3. Replaying the same seed produces the same decision and route.
4. An Agent committed to useful cover selects `lean_cover` at score 96.
5. An unusable committed lane selects `leave_cover`.
6. A central blocker produces a flank that opens LOS while preserving attack
   AP.
7. The integrated `AIBehavior` surface emits the canonical cover-route and
   simple-flank rationale signatures.

These are deterministic source tests, not a claim that an arbitrary live
mission must happen to exercise every branch.

## Verification

| Gate | Result |
|---|---|
| M03 golden checks | PASS, 12/12 |
| PlaytestRunner at M03 closure | PASS, 259/259 |
| Current combined PlaytestRunner | PASS, 261/261 |
| `AITactics.gd` changes required | none |
| `AIBehavior.gd` changes required | none |

The known exit-time ObjectDB/resource warnings remained after successful
runner completion.

## Honest boundary

The golden tests close the deterministic source-logic gate. A live atomic
browser run may report no M03 cover/flank rationale if that particular mission
never presents the required geometry; absence in one run is not treated as a
source failure. Live ledger evidence remains an observational gate.
